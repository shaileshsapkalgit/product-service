server:
  port: 8081

spring:
  application:
    name: product-service

  datasource:
    url: jdbc:mysql://productorder-db.c47aa2qaslgg.us-east-1.rds.amazonaws.com:3306/productdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: admin
    password: Confirm2002#tcs
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
